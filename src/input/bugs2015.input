)set break resume
)expose UnittestCount UnittestAux Unittest

testsuite "bugs2015"

testcase "bug in lifting"

testTrue("(res := factor((4*x^3+2*y^2+1)*(12*x^5-x^3*y+12)*(y^2+3)); true)")
fl := factors(res);
testEquals("#fl", "3")
testEquals("fl(1).factor", "x^3*y - 12*x^5 - 12")

)set output algebra on
statistics()
